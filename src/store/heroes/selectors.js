export const getHeroes = (state) => state.heroes.heroes;

export const getHero = (state) => state.heroes.hero;

export const getSelectedHeroes = (state) => state.heroes.selectedHeroes;

export const getIsLoading = (state) => state.heroes.isLoading;

export const getIsSearching = (state) => state.heroes.isSearching;

export const getError = (state) => state.heroes.error;

export const getErrorSearch = (state) => state.heroes.errorSearch;
